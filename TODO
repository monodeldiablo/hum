* implement add icon on search result hover
* implement tooltips over view cells, so that users can see full titles, etc.
* clean up the search results pane expand/shrink behavior
  * only expand the window for as many results as there are, up to 5
  * provide a red flash or some other feedback in the background of the search entry if no results
  * add asynchronous search
  * provide search progress feedback in the background of the search entry
  * paginate results to tighten memory footprint
~ create "about" dialog
* fix (if possible) the various warnings during compilation
* extract the search/playlist view code into separate files (maybe)
* clean up/simplify/condense the signal & callback code
* more thoroughly comment hum_gui.vala
* figure out how to manage right-click behavior
* figure out what to put in the menubar
  * implement playlist clearing (probably best through a menu)
* implement a tray icon (separate front end?)
* register hum-gtk to be the default handler for audio mime types
* clean up and finalize installation bundle for packaging
* investigate using DecodeBin for crossfading (what impact does this have on playlist?)
* check & reduce memory footprint of hum-gtk and hum-player
* stick persistent settings in GConf
* investigate volume control (not necessary for a first release)
* implement better shuffle
* investigate the benefits of using a UI manager
* log information to a standard location (in ~/.local/share/hum/hum-{gtk,player}.log)
* clean up HumQueryEngine and apply some sanity to its methods and design
* investigate taglib
* investigate "collate_key()" in the sorting code
